{% extends '::base.html.twig' %}

{% block body -%}
    <h1>role edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('role') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
