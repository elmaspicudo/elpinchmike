{% extends '::base.html.twig' %}

{% block body -%}
    <h1>file creation</h1>

    {{ form(form) }}

        <a  href="{{ path('file') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
