{% extends '::base.html.twig' %}

{% block body -%}
    <h1>file edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('file') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
