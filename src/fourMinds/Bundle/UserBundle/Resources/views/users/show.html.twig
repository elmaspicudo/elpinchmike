{% extends'::base.html.twig' %}
{% block placeholder -%}
<div class="row">
    <div class="col-lg-12">
        <ol class="breadcrumb">
            <li><a href="#">Home</a></li>
            <li class="active"><span>Clientes</span></li>
        </ol>        
        <div class="clearfix">
            <h1 class="pull-left">Cliente</h1>
            <div class="pull-right top-page-ui">
                <a href="{{ path('cliente_new')}}" class="btn btn-primary pull-right">
                    <i class="fa fa-plus-circle fa-lg"></i> Nuevo Cliente
                </a>
            </div>
        </div>
        
    </div>
</div>
{% endblock -%}
{% block body -%}
<div class="row">
<div class="col-lg-12">
<div class="main-box no-header clearfix">
<div class="main-box-body clearfix">
<div class="table-responsive">
<table class="table user-list table-hover">
                        <tbody>
                            <tr>
                                <th>Email</th>
                                <td>{{ entity.email }}</td>
                            </tr>
                            
                        </tbody>
                    </table>
                   <div class="">
                        <a href="{{ path('users') }}" class="btn btn-primary pull-right">
                            <i class="fa fa-mail-forward fa-lg"></i> Regresar
                        </a>
                        {{ form_start(delete_form) }}
                            <button type="submit" class="btn btn-primary">
                                <i class="fa fa-trash-o fa-lg"></i> Eliminar
                            </button>
                        {{ form_end(delete_form) }}
                   </div>
                                           
            
</div>
</div>
</div>
</div>
  
    {% endblock %}