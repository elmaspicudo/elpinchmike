{% extends '::base.html.twig' %}

{% block body -%}
    <h1>tiempoEtapa</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Etapa</th>
                <td>{{ entity.etapa }}</td>
            </tr>
            <tr>
                <th>Tiempo</th>
                <td>{{ entity.tiempo }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tiempoetapa') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('tiempoetapa_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
