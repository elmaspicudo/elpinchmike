{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Bienes_autos edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('Bienes_autos') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
