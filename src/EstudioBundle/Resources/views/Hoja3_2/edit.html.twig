{% extends '::base.html.twig' %}

{% block body -%}
    <h1>personasDomicilio edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('personasDomicilio') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
