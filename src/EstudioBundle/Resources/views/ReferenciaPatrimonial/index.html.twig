{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ReferenciaPatrimonial list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Tipo</th>
                <th>Domicilio</th>
                <th>Valorestimado</th>
                <th>Liquidado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('referenciapatrimonial_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.tipo }}</td>
                <td>{{ entity.domicilio }}</td>
                <td>{{ entity.valorEstimado }}</td>
                <td>{{ entity.liquidado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('referenciapatrimonial_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('referenciapatrimonial_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('referenciapatrimonial_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
