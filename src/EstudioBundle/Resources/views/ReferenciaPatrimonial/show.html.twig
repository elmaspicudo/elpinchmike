{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ReferenciaPatrimonial</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ entity.tipo }}</td>
            </tr>
            <tr>
                <th>Domicilio</th>
                <td>{{ entity.domicilio }}</td>
            </tr>
            <tr>
                <th>Valorestimado</th>
                <td>{{ entity.valorEstimado }}</td>
            </tr>
            <tr>
                <th>Liquidado</th>
                <td>{{ entity.liquidado }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('referenciapatrimonial') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('referenciapatrimonial_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
