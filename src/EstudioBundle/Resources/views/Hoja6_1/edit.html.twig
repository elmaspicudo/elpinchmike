{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hoja6_1 edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('hoja6_1') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
