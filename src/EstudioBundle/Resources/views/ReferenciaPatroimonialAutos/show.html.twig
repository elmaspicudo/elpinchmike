{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ReferenciaPatroimonialAutos</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Marca</th>
                <td>{{ entity.marca }}</td>
            </tr>
            <tr>
                <th>Modelo</th>
                <td>{{ entity.modelo }}</td>
            </tr>
            <tr>
                <th>Valorestimado</th>
                <td>{{ entity.valorEstimado }}</td>
            </tr>
            <tr>
                <th>Liquidado</th>
                <td>{{ entity.liquidado }}</td>
            </tr>
        </tbody>
    </table>

        <a  href="{{ path('referenciapatroimonialautos') }}" class="btn btn-warning">Regresar</a>
        <a href="{{ path('referenciapatroimonialautos_edit', { 'id': entity.id }) }}" class="btn btn-success">Editar</a>
    {{ form(delete_form) }}
{% endblock %}
