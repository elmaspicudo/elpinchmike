{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ReferenciaPatroimonialAutos list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Marca</th>
                <th>Modelo</th>
                <th>Valorestimado</th>
                <th>Liquidado</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('referenciapatroimonialautos_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.marca }}</td>
                <td>{{ entity.modelo }}</td>
                <td>{{ entity.valorEstimado }}</td>
                <td>{{ entity.liquidado }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('referenciapatroimonialautos_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('referenciapatroimonialautos_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('referenciapatroimonialautos_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
