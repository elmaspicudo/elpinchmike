{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ingresos edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('ingresos') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
