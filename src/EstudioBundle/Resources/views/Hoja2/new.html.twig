{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hoja2 creation</h1>

    {{ form(form) }}

        <a  href="{{ path('hoja2') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
