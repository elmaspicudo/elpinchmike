{% extends '::base.html.twig' %}

{% block body -%}
    <h1>DeudasActualesH5</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Banco</th>
                <td>{{ entity.banco }}</td>
            </tr>
            <tr>
                <th>Limitecredito</th>
                <td>{{ entity.limiteCredito }}</td>
            </tr>
            <tr>
                <th>Antiguedad</th>
                <td>{{ entity.antiguedad }}</td>
            </tr>
            <tr>
                <th>Pagomensual</th>
                <td>{{ entity.pagoMensual }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('deudasactualesh5') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('deudasactualesh5_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
