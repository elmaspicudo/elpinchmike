{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hoja5_3</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Banco</th>
                <td>{{ entity.banco }}</td>
            </tr>
            <tr>
                <th>Limitecredito</th>
                <td>{{ entity.limiteCredito }}</td>
            </tr>
            <tr>
                <th>Antiguedad</th>
                <td>{{ entity.antiguedad|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Pagomensual</th>
                <td>{{ entity.pagoMensual|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('hoja5_3') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('hoja5_3_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
