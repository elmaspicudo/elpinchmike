{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hoja3_3 list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Edad</th>
                <th>Parentesco</th>
                <th>Estadocivil</th>
                <th>Escolaridad</th>
                <th>Ocupacion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('hoja3_3_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.edad }}</td>
                <td>{{ entity.parentesco }}</td>
                <td>{{ entity.estadoCivil }}</td>
                <td>{{ entity.escolaridad }}</td>
                <td>{{ entity.ocupacion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('hoja3_3_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('hoja3_3_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('hoja3_3_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
