{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hoja5_6</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Aseguradora</th>
                <td>{{ entity.aseguradora }}</td>
            </tr>
            <tr>
                <th>Tiposeguro</th>
                <td>{{ entity.tipoSeguro }}</td>
            </tr>
            <tr>
                <th>Mensualidad</th>
                <td>{{ entity.mensualidad }}</td>
            </tr>
            <tr>
                <th>Vigencia</th>
                <td>{{ entity.vigencia|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('hoja5_6') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('hoja5_6_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
