{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hoja5_6 edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('hoja5_6') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
