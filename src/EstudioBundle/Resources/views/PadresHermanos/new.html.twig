{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PadresHermanos creation</h1>

    {{ form(form) }}

        <a  href="{{ path('padreshermanos') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
