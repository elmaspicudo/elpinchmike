{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PadresHermanos edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('padreshermanos') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
