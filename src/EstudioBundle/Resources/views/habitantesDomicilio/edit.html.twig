{% extends '::base.html.twig' %}

{% block body -%}
    <h1>habitantesDomicilio edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('habitantesdomicilio') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
