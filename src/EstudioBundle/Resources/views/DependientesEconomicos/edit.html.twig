{% extends '::base.html.twig' %}

{% block body -%}
    <h1>DependientesEconomicos edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('dependienteseconomicos') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
