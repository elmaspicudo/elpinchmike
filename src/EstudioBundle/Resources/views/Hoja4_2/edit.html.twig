{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hoja4_2 edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('hoja4_2') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
