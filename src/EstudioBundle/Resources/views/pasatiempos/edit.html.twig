{% extends '::base.html.twig' %}

{% block body -%}
    <h1>pasatiempos edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('pasatiempos') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
