{% extends '::base.html.twig' %}

{% block body -%}
    <h1>referenciasLaborales edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('referenciasLaborales') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
