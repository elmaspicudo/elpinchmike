{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Imagenes_1 edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('Imagenes_1') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
