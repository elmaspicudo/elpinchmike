{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Bienes_raices edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('Bienes_raices') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
