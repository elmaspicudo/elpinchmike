{% extends '::base.html.twig' %}

{% block body -%}
    <h1>EsposaHijos edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('esposahijos') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
