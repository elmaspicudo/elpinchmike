{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hoja5_5</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Institucion</th>
                <td>{{ entity.institucion }}</td>
            </tr>
            <tr>
                <th>Monto</th>
                <td>{{ entity.monto }}</td>
            </tr>
            <tr>
                <th>Pagomensual</th>
                <td>{{ entity.pagoMensual }}</td>
            </tr>
            <tr>
                <th>Saldoactual</th>
                <td>{{ entity.saldoActual }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('hoja5_5') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('hoja5_5_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
