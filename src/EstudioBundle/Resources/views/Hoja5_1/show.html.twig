{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hoja5_1</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Investigado</th>
                <td>{{ entity.investigado }}</td>
            </tr>
            <tr>
                <th>Conyuge</th>
                <td>{{ entity.conyuge }}</td>
            </tr>
            <tr>
                <th>Padres</th>
                <td>{{ entity.padres }}</td>
            </tr>
            <tr>
                <th>Hermanos</th>
                <td>{{ entity.hermanos }}</td>
            </tr>
            <tr>
                <th>Otros</th>
                <td>{{ entity.otros }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('hoja5_1') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('hoja5_1_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
