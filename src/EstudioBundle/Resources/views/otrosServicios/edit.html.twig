{% extends '::base.html.twig' %}

{% block body -%}
    <h1>otrosServicios edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('otrosServicios') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
