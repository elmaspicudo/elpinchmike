{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hoja3_4 list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Edad</th>
                <th>Parentesco</th>
                <th>Estadocivil</th>
                <th>Escolaridad</th>
                <th>Ocupacion</th>
                <th>Salario</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('hoja3_4_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.nombre }}</td>
                <td>{{ entity.edad }}</td>
                <td>{{ entity.parentesco }}</td>
                <td>{{ entity.estadoCivil }}</td>
                <td>{{ entity.escolaridad }}</td>
                <td>{{ entity.ocupacion }}</td>
                <td>{{ entity.salario }}</td>
                <td>
                <a  href="{{ path('hoja3_4_show', { 'id': entity.id }) }}" class="table-link">
	<span class="fa-stack">
	<i class="fa fa-square fa-stack-2x"></i>
	<i class="fa fa-search-plus fa-stack-1x fa-inverse"></i>
	</span>
</a>
                    <a href="{{ path('hoja3_4_edit', { 'id': entity.id }) }}" class="table-link">
	<span class="fa-stack">
	<i class="fa fa-square fa-stack-2x"></i>
	<i class="fa fa-pencil fa-stack-1x fa-inverse"></i>
	</span>
</a>
                    </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('hoja3_4_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
