{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Referencia_Personal</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Observaciones</th>
                <td>{{ entity.observaciones }}</td>
            </tr>
        </tbody>
    </table>

        <a  href="{{ path('Referencia_Personal') }}" class="btn btn-warning">Regresar</a>
        <a href="{{ path('Referencia_Personal_edit', { 'id': entity.id }) }}" class="btn btn-success">Editar</a>
    {{ form(delete_form) }}
{% endblock %}
