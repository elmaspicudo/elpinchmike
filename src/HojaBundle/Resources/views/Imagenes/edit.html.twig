{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Imagenes edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('Imagenes') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
