{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Referencia_laboral edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('Referencia_laboral') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
