{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hoja9 edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('hoja9') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
