{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Hoja6 edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('hoja6') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
