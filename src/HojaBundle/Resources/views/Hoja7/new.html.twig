{% extends '::base.html.twig' %}

{% block body -%}
<style>
  .btn-primary{
    width: 200px!important;
  }
</style>
<div class="div col-md-12 main-box no-header clearfix">

    <h1 class="text-center"><strong>Referencias Personales </strong> </h1>

    {{ form_start(form) }}

    <div class="col-md-12">REFERENCIA FAMILIAR</div>
    <ul  id="hoja7_1" data-prototype="{{ form_widget(form.hoja7_1.vars.prototype) | e }}">
        {% for hoja71 in form.hoja7_1 %}
        <div class="col-md-12">
            {{ form_errors(hoja71) }}
            {{ form_widget(hoja71) }}
        </div> 
        {% endfor %}
    </ul>    
    <a href="#" class="col-md-4 col-md-offset-4 btn btn-info pull-left" id="add-hoja71">Agregar Referencia Familiar</a>  
     <div class="col-md-12">REFERENCIA </div>
    <ul  id="hoja7_2" data-prototype="{{ form_widget(form.hoja7_2.vars.prototype) | e }}">
        {% for hoja72 in form.hoja7_2 %}
        <div class="col-md-12">
            {{ form_errors(hoja72) }}
            {{ form_widget(hoja72) }}
        </div> 
        {% endfor %}
    </ul>    
    <a href="#" class="col-md-4 col-md-offset-4 btn btn-info pull-left" id="add-hoja72">Agregar Referencia</a>  
    <div class="col-md-12">SALUD E HIGIENE</div>
    <ul  id="hoja7_3" data-prototype="{{ form_widget(form.hoja7_3.vars.prototype) | e }}">
        {% for hoja73 in form.hoja7_3 %}
        <div class="col-md-12">
            {{ form_errors(hoja73) }}
            {{ form_widget(hoja73) }}
        </div> 
        {% endfor %}
    </ul>    
    <a href="#" class="col-md-4 col-md-offset-4 btn btn-info pull-left" id="add-hoja73">Agregar Padecimientos</a>    
    <div class="col-md-12">DESARROLLO Y PASATIEMPOS</div>
    <ul  id="hoja7_4" data-prototype="{{ form_widget(form.hoja7_4.vars.prototype) | e }}">
        {% for hoja74 in form.hoja7_4 %}
        <div class="col-md-12">
            {{ form_errors(hoja74) }}
            {{ form_widget(hoja74) }}
        </div> 
        {% endfor %}
    </ul>    
    <a href="#" class="col-md-4 col-md-offset-4 btn btn-info pull-left" id="add-hoja74">Agregar Pasatiempo</a>
    {{ form_end(form) }}
</div>
	<div class="col-md-12">
	<a href="{{path('hoja8')}}" class="btn btn-primary pull-right">
	<i class="fa fa-arrow-circle-right  fa-lg"></i> Siguiente
	</a>
	<a href="{{path('hoja6')}}" class="btn btn-primary pull-left">
	<i class="fa fa-arrow-circle-left  fa-lg"></i> Anterior
	</a>
	</div>
     <script>

     var emailCount = '{{ form.hoja7_1 | length }}';
     var emailCount1 = '{{ form.hoja7_2 | length }}';
     var emailCount2 = '{{ form.hoja7_3 | length }}';
     var emailCount3 = '{{ form.hoja7_4 | length }}';
     $('div.col-lg-6:nth-child(13)').hide()
     $('div.col-lg-6:nth-child(14)').hide()
     $('div.col-lg-6:nth-child(15)').hide()
     $('div.col-lg-6:nth-child(16)').hide()

    jQuery(document).ready(function() {
        jQuery('#add-hoja71').click(function() {
            var emailList = jQuery('#hoja7_1');

            // graba la plantilla prototipo
            var newWidget = emailList.attr('data-prototype');
            // sustituye el "__name__" usado en el id y name del prototipo con un
            // número que es único en tus correos termina con el
            // atributo name viéndose como name="contact[emails][2]"
            newWidget = newWidget.replace(/__name__/g, emailCount);
            emailCount++;
            // crea un nuevo elemento lista y lo añade a la lista
            var newLi = jQuery('<div></div>').html(newWidget);
            newLi.appendTo(jQuery('#hoja7_1'));

            return false;
        });


        jQuery('#add-hoja72').click(function() {
            var emailList1 = jQuery('#hoja7_2');

            // graba la plantilla prototipo
            var newWidget1 = emailList1.attr('data-prototype');
            // sustituye el "__name__" usado en el id y name del prototipo con un
            // número que es único en tus correos termina con el
            // atributo name viéndose como name="contact[emails][2]"
            newWidget1 = newWidget1.replace(/__name__/g, emailCount1);
            emailCount1++;
            // crea un nuevo elemento lista y lo añade a la lista
            var newLi1 = jQuery('<div></div>').html(newWidget1);
            newLi1.appendTo(jQuery('#hoja7_2'));

            return false;
        });



        jQuery('#add-hoja73').click(function() {
            var emailList2 = jQuery('#hoja7_3');

            // graba la plantilla prototipo
            var newWidget2 = emailList2.attr('data-prototype');
            // sustituye el "__name__" usado en el id y name del prototipo con un
            // número que es único en tus correos termina con el
            // atributo name viéndose como name="contact[emails][2]"
            newWidget2 = newWidget2.replace(/__name__/g, emailCount2);
            emailCount2++;
            // crea un nuevo elemento lista y lo añade a la lista
            var newLi2 = jQuery('<div></div>').html(newWidget2);
            newLi2.appendTo(jQuery('#hoja7_3'));

            return false;
        });



        jQuery('#add-hoja74').click(function() {
            var emailList3 = jQuery('#hoja7_4');

            // graba la plantilla prototipo
            var newWidget3 = emailList3.attr('data-prototype');
            // sustituye el "__name__" usado en el id y name del prototipo con un
            // número que es único en tus correos termina con el
            // atributo name viéndose como name="contact[emails][2]"
            newWidget3 = newWidget3.replace(/__name__/g, emailCount3);
            emailCount3++;
            // crea un nuevo elemento lista y lo añade a la lista
            var newLi3 = jQuery('<div></div>').html(newWidget3);
            newLi3.appendTo(jQuery('#hoja7_4'));

            return false;
        });
      })
  </script>
{% endblock %}
