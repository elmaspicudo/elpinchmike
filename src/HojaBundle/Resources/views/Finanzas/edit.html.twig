{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Finanzas edit</h1>

    {{ form(edit_form) }}

        <a  href="{{ path('Finanzas') }}">
            Back to the list
        </a>
    </li>
    {{ form(delete_form) }}
{% endblock %}
